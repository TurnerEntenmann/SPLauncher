version: "2.0"

rules:

- rule: use spSearcher.exe on a file
  steps:
  - intent: fsearch
  - action: fsearch_action
  
- rule: use spearcher.exe on an app
  steps:
  - intent: asearch
  - action: asearch_action
