version: "2.0"

intents:
  - fsearch
  - asearch

entities:
  - file_name
  - app_name
  
actions:
  - fsearch_action
  - asearch_action

slots:
  file_name:
    type: text
  app_name:
    type: text


session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
